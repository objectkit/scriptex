import { Scriptex } from "@objectkit/scriptex"

const sandbox = sinon.createSandbox()
describe(`Scriptex`, () => {

  beforeEach(()=>{})

  afterEach(()=>{})
  
  describe(`static SYSTEM:Scripter`, () => {  })

  describe(`static API:Map<string,string>`, () => { })

  describe(`new Scriptex(system:Object, api:Map<string,string>, configurable:boolean)`,()=>{})

  describe(`deploy(plugin:Object):Array<string>`, () => {})
})
