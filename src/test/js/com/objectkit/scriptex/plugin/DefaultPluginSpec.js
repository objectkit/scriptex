import { DefaultPlugin } from "@objectkit/scriptex"
import { Scripter } from "@objectkit/scriptex-mocks"

describe(`DefaultPlugin`, () => {
  context(`static deploy(Object:system, configurable:boolean):Array<string>`, () => {
    specify(`stub`, () => {

    })
  })
})
