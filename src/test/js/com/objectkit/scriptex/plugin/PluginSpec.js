import { Scriptex, Plugin } from "@objectkit/scriptex"

const sandbox = sinon.createSandbox()
describe(`Plugin`, () => {
  
  beforeEach(()=>{})

  afterEach(()=>{})

  describe(`static API:Map<string,string>`, () =>{ })

  describe(`static deploy(...ctorArgs):Array<string>`, () => { })

  describe(`#init():void`, () => { })

  describe(`#system`, () => {})
})
